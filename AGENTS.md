# 26matchF4 工程开发要求

本文件适用于整个仓库。以后新增或修改代码时，必须遵守以下要求。

## 代码注释

- 新增代码必须包含必要的中文注释，说明代码的用途和设计意图。
- 新增函数应写函数说明，至少说明：
  - 函数完成的功能；
  - 参数和返回值的含义；
  - 调用前提，例如外设初始化、电机使能或必须已有有效反馈；
  - 是否包含循环、延时或其他阻塞行为；
  - 可能的退出条件、失败情况和安全影响。
- 关键变量应注明单位、有效范围和数据来源。例如角度使用 `rad` 还是 `deg`、速度使用 `rad/s`，以及数据来自编码器、CAN 反馈还是视觉串口。
- 电机代码应明确零点、正负方向、机械限位与电控限位；修改限位值时应注明对应电机和基准零点。
- 通信代码应说明接口、报文 ID、数据长度、字节序、帧格式、接收方式以及发送或采样周期。
- 中断、DMA、回调和共享变量应说明更新位置及并发访问注意事项；需要时正确使用 `volatile`。
- 对状态机、边界判断、安全保护、错误恢复和不容易直接看懂的算法添加原因说明。
- 不要求给每一条赋值或简单语句重复写注释，避免产生与代码同义、不能帮助理解的无效注释。
- 修改实现时同步更新相关注释，禁止保留已经与代码行为不一致的注释。

## 文件和工程结构

- 业务模块放在 `User` 下对应目录，头文件放入 `Inc`，实现文件放入 `Src`。
- 新增源文件后，将其加入 `MDK-ARM/26matchF4.uvprojx` 的对应工程分组。
- 修改 CubeMX 生成文件时，业务代码尽量放在对应的 `USER CODE BEGIN/END` 区域内，避免重新生成工程后丢失。
- 公共接口在头文件中声明；只在单个源文件内使用的函数和变量优先声明为 `static`。

## 嵌入式安全要求

- 电机、执行器和功率输出相关代码必须进行范围检查，并为真实机械边界保留安全余量。
- 仅限制目标位置不等同于完整限位；存在速度或力矩控制时，还要考虑实际位置越界后的输出方向。
- 阻塞循环必须写明退出条件。依赖传感器或总线反馈退出时，应考虑反馈未到达、数据失效或通信中断的情况。
- 周期发送 CAN、串口等报文时应设置合理周期，避免无延时循环占满总线或发送邮箱。
- 对可能导致设备持续动作的通信故障、非法数据和超时情况，应优先进入可控的安全状态。

## 修改和交付

- 保留用户已有且与当前任务无关的修改，不随意覆盖或格式化其他文件。
- 完成代码后进行与改动风险相匹配的编译或静态检查。
- 交付时说明修改的文件、调用方法、必要前提以及已经完成的验证。
